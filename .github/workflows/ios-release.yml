name: iOS Release

on:
  push:
    branches: [main]
    paths:
      - 'ios/**'
      - '.github/workflows/ios-release.yml'

# TestFlight rejects a build number it has already processed, and the
# commit-count build number only grows with main's history, so runs must land
# in order. Serialize instead of canceling: a killed run after the upload
# started could leave App Store Connect mid-processing.
concurrency:
  group: ios-release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  testflight:
    # Gated separately from the macOS pipeline so the two can be enabled
    # independently: macOS ships via Developer ID and needs no App Store
    # presence, while TestFlight uploads need an App Store Connect app record.
    # Skips cleanly until the one-time Apple setup (docs/deployment.md,
    # "iOS Release Pipeline") has populated both repository variables.
    if: vars.IOS_TESTFLIGHT_ENABLED != '' && vars.APPLE_TEAM_ID != ''
    runs-on: macos-15
    defaults:
      run:
        working-directory: ios
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
        with:
          # The build number is the commit count, which needs full history.
          fetch-depth: 0

      - name: Install fastlane
        run: bundle install

      - name: Build and upload to TestFlight
        run: bundle exec fastlane beta
        env:
          APPLE_TEAM_ID: ${{ vars.APPLE_TEAM_ID }}
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_CONTENT: ${{ secrets.ASC_KEY_CONTENT }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
