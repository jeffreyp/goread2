name: Production Environment Smoke Test (temporary)

on:
  workflow_dispatch:

jobs:
  test-gate:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "If you can see this, the approval gate was passed."
